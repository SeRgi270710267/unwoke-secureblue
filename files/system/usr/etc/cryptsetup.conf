# Default for any later LUKS format on the installed OS.
# Live USB also writes this from prep_rootfs.sh so Anaconda/blivet pick it up.
# Memory is KiB. Revert: edit or remove /etc/cryptsetup.conf
pbkdf = argon2id
pbkdf-memory = 2097152
