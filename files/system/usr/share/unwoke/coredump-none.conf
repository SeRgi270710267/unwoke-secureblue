# Unwoke SecureBlue. Not affiliated. UNWOKE-SHIPPED-FIRST.
# Core dumps are a RAM snapshot on disk. Stock already prefers off; fail-closed here.
[Coredump]
Storage=none
ProcessSizeMax=0
ExternalSizeMax=0
JournalSizeMax=0
