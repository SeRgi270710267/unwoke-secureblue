# Unwoke SecureBlue. Not affiliated. UNWOKE-SHIPPED-FIRST.
# Hibernate writes RAM (keys, session) to disk. Stock zram already cannot hibernate well.
[Login]
AllowHibernation=no
AllowHybridSleep=no
AllowSuspendThenHibernate=no
HibernateKeyIgnoreInhibited=no
